---
interface Props {
  title: string;
  description: string[];
  link: string;
  date: string;
}

const { title, description, link, date } = Astro.props;
---

<div class="w-full">
  <div class="ml-9">
    <div class="w-full flex justify-start gap-6 border-neutral-800 border-l pb-16">
      <div class="relative">
        <div
          class="absolute top-[-2px] left-[-8.5px] w-4 h-4 rounded-full aspect-square outline-black bg-purple-500"
        >
          <div
            class="absolute top-0 left-0 rounded-full -z-10 w-4 h-4 bg-purple-500 animate-ping aspect-square"
          ></div>
        </div>
      </div>
      <div class="mt-[-4px] flex flex-col gap-2">
        <p class="text-base text-gray-400">
          <a
            href={link}
            target="_blank"
            rel="noopener noreferrer"
            class="hover:text-white hover:underline transition-all ease"
          >
            {title}
          </a>
          {date}
        </p>
        {description.map((paragraph) => (
          <p class="mb-3 text-pretty">{paragraph}</p>
        ))}
      </div>
    </div>
  </div>
</div>
